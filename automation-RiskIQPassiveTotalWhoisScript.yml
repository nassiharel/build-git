args:
- default: true
  description: domain or email indicator value that need to enrich
  name: indicator_value
  required: true
comment: Enhancement script to enrich whois information for Domain and Email type
  of indicators.
commonfields:
  id: RiskIQPassiveTotalWhoisScript
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: 5.0.0
    itemVersion: 2.1.1
    packID: PassiveTotal
    packName: PassiveTotal
    packPropagationLabels:
    - all
    propagationLabels: []
    toServerVersion: ""
dependson:
  must:
  - '|||pt-whois-search'
dockerimage: demisto/python3:3.9.7.24076
enabled: true
engineinfo: {}
mainengineinfo: {}
name: RiskIQPassiveTotalWhoisScript
pswd: ""
runas: DBotWeakRole
runonce: false
script: |
  register_module_line('RiskIQPassiveTotalWhoisScript', 'start', __line__())


  indicator_value = demisto.args().get('indicator_value')

  indicator_type = 'domain'
  if re.match(emailRegex, indicator_value):
      indicator_type = 'email'

  result = demisto.executeCommand('pt-whois-search', {'field': indicator_type, 'query': indicator_value})

  demisto.results(result)

  register_module_line('RiskIQPassiveTotalWhoisScript', 'end', __line__())
scripttarget: 0
subtype: python3
system: true
tags:
- enhancement
type: python
